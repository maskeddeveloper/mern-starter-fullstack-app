{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for the the front-end client build using ReactJS. this backend is using MongoDB,React,Express and Node.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Masked Developer",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mongoose": "^5.4.7",
    "morgan": "^1.9.1"
  }
}
